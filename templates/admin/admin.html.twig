<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>{% block title %}MonSite Recettes{% endblock %}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/choices.js/public/assets/styles/choices.min.css" />
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Open+Sans&display=swap" rel="stylesheet" />

    {% block stylesheets %}
            {{ encore_entry_link_tags('admin') }}

{% endblock %}
{% block javascripts %}  
            {{ encore_entry_script_tags('admin') }}

{% endblock %}
</head>

  <body class="{{ body_class|default('') }}">
      <nav class="navbar navbar-expand-lg navbar-light sticky-top shadow-sm">
        <div class="container">

          <!-- Logo -->
          <a class="navbar-brand" href="{{ path('admin.recipe.index') }}">Administration</a>

          <!-- Bouton hamburger pour mobile -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
            aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Contenu du menu -->
          <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav mx-auto">

              <!-- Accueil -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') == 'home' ? 'active' : '' }}" href="{{ path('home') }}">
                  Accueil
                </a>
              </li>

              <!-- Recettes (dropdown amélioré) -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'admin.recipe.' ? 'active' : '' }}" 
                href="{{ path('admin.recipe.index') }}">
                    Recettes
                </a>
              </li>

              <!-- Catégories -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'admin.category.' ? 'active' : '' }}"
                  href="{{ path('admin.category.index') }}">
                  Catégories
                </a>
              </li>

              <!-- Administration -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'admin.users.' ? 'active' : '' }}" href="{{ path('admin.users.index') }}">Utilisateurs</a>
              </li>
            </ul>

            <!-- Recherche -->
            <form class="d-flex me-3" role="search" method="GET" action="#">
              <button class="btn btn-outline-success" type="submit"><i class="fas fa-search"></i></button>
            </form>

            <!-- Utilisateur connecté / non connecté -->
            <div class="d-flex align-items-center nav-user-text">
                {% if app.user %}
                    <div class="dropdown user-dropdown position-static">
                        <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown"
                          aria-expanded="false">
                          {{ app.user.username|default('Profil')|title }}
                        </a>
                        <div class="dropdown-menu">
                            <div class="row g-3">
                                <div class="col-12">
                                    <h6><i class="fas fa-user-circle"></i> Mon compte</h6>
                                    <ul class="list-unstyled">
                                        <li><a class="dropdown-item" href="{{ path('user.profile') }}">Mon profil</a></li>
                                        <li><a class="dropdown-item" href="{{ path('user.recipe.my_recipes') }}">Mes recettes</a></li>
                                        <li><hr class="dropdown-divider"></li>
                                        <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-2">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-primary">Inscription</a>
                {% endif %}
            </div>
          </div>
        </div>
      </nav>
      

      <!-- Main content -->
      <main>
      {% include 'partials/flash.html.twig' %}
        {% block body %}{% endblock %}
      </main>
      <footer class="mt-5 pt-5 bg-footer text-dark">
      <div class="container">

        <!-- Newsletter -->
        <div class="row justify-content-center text-center mb-5">
          <div class="col-md-8">
            <h4 class="fw-bold mb-3">🌱 Abonnez-vous à notre newsletter</h4>
            <p class="mb-4 text-muted">Recevez chaque semaine des idées de recettes saines et gourmandes directement dans votre boîte mail.</p>
            <form class="d-flex flex-column flex-sm-row justify-content-center gap-2">
              <input type="email" class="form-control" placeholder="Votre email" required>
              <button class="btn btn-success px-4" type="submit">S'abonner</button>
            </form>
          </div>
        </div>

        <!-- Grille de liens -->
        <div class="row justify-content-center text-center row-cols-1 row-cols-md-4 g-4 pb-5">
          <div class="col">
            <h5 class="text-success">Découvrir</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">Recettes</a></li>
              <li><a href="#" class="footer-link">Catégories</a></li>
              <li><a href="#" class="footer-link">Nouveautés</a></li>
              <li><a href="#" class="footer-link">Saison</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">À propos</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">Notre histoire</a></li>
              <li><a href="#" class="footer-link">L’équipe</a></li>
              <li><a href="#" class="footer-link">Contact</a></li>
              <li><a href="#" class="footer-link">Presse</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">Aide</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">FAQ</a></li>
              <li><a href="#" class="footer-link">Support</a></li>
              <li><a href="#" class="footer-link">Mentions légales</a></li>
              <li><a href="#" class="footer-link">Politique de confidentialité</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">Suivez-nous</h5>
            <div class="d-flex justify-content-center gap-3 mt-2">
              <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-tiktok"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-x-twitter"></i></a>
            </div>
          </div>
        </div>
      </div>

      <!-- Bas de page -->
      <div class="text-center py-3 bg-light border-top">
        <small class="text-muted">© {{ "now"|date("Y") }} MonSite Recettes — Tous droits réservés.</small>
      </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
        crossorigin="anonymous"></script>
  </body>
</html>



