oui <!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>{% block title %}MonSite Recettes{% endblock %}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

  <!-- Bootstrap & FontAwesome -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Open+Sans&display=swap" rel="stylesheet" />

    {% block stylesheets %}
      {{ encore_entry_link_tags('app') }}  
  {% endblock %}
     <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
{% block javascripts %}  
            {{ encore_entry_script_tags('app') }}

{% endblock %}
</head>

  <body class="{{ body_class|default('') }}">
      <nav class="navbar navbar-expand-lg navbar-light sticky-top shadow-sm">
        <div class="container">

          <!-- Logo -->
          <a class="navbar-brand" href="{{ path('home') }}">Mes Recettes</a>

          <!-- Bouton hamburger pour mobile -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
            aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <!-- Contenu du menu -->
          <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav mx-auto">

              <!-- Accueil -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') == 'home' ? 'active' : '' }}" href="{{ path('home') }}">
                  Accueil
                </a>
              </li>

              <!-- Recettes (dropdown amélioré) -->
              <li class="nav-item dropdown position-static">
              <a class="nav-link dropdown-toggle" href="#" id="recettesDropdown" role="button" data-bs-toggle="dropdown"
                      aria-expanded="false">
                  Recettes
              </a>
              <div class="dropdown-menu w-100 p-4" aria-labelledby="recettesDropdown"
                  style="background-color: #f5fff8; border-radius: 0 0 8px 8px; box-shadow: 0 4px 15px rgba(0,0,0,0.1);">
                  <div class="row gx-5 gy-4">

                  <!-- Colonne 1 -->
                  <div class="col-12 col-md-3">
                      <h6 class="text-success fw-bold mb-3">
                      <i class="fas fa-book-open me-2"></i> Toutes les recettes
                      </h6>
                      <ul class="list-unstyled">
                      <li><a class="dropdown-item py-1" href="{{ path('admin.recipe.index') }}">Toutes les recettes</a></li>
                      <li><a class="dropdown-item py-1" href="#">Recettes populaires</a></li>
                      <li><a class="dropdown-item py-1" href="#">Recettes végétariennes</a></li>
                      <li><a class="dropdown-item py-1" href="#">Recettes de saison</a></li>
                      </ul>
                  </div>

                  <!-- Colonne 2 -->
                  <div class="col-12 col-md-3">
                      <h6 class="text-success fw-bold mb-3">
                      <i class="fas fa-seedling me-2"></i> Par catégorie
                      </h6>
                      <ul class="list-unstyled">
                      <li><a class="dropdown-item py-1" href="#">Entrées</a></li>
                      <li><a class="dropdown-item py-1" href="#">Plats principaux</a></li>
                      <li><a class="dropdown-item py-1" href="#">Desserts</a></li>
                      <li><a class="dropdown-item py-1" href="#">Boissons</a></li>
                      </ul>
                  </div>

                  <!-- Colonne 3 -->
                  <div class="col-12 col-md-3">
                      <h6 class="text-success fw-bold mb-3">
                      <i class="fas fa-clock me-2"></i> Spécialités
                      </h6>
                      <ul class="list-unstyled">
                      <li><a class="dropdown-item py-1" href="#">Rapide & facile</a></li>
                      <li><a class="dropdown-item py-1" href="#">Sans gluten</a></li>
                      <li><a class="dropdown-item py-1" href="#">Vegan</a></li>
                      <li><a class="dropdown-item py-1" href="#">Sans lactose</a></li>
                      </ul>
                  </div>

                  <!-- Colonne 4 -->
                  <div class="col-12 col-md-3">
                      <h6 class="text-success fw-bold mb-3">
                      <i class="fas fa-leaf me-2"></i> Par saisons
                      </h6>
                      <ul class="list-unstyled">
                      <li><a class="dropdown-item py-1" href="#">Printemps</a></li>
                      <li><a class="dropdown-item py-1" href="#">Été</a></li>
                      <li><a class="dropdown-item py-1" href="#">Automne</a></li>
                      <li><a class="dropdown-item py-1" href="#">Hiver</a></li>
                      </ul>
                  </div>

                  </div>
              </div>
              </li>

              <!-- Catégories -->
              <li class="nav-item">
                <a class="nav-link {{ app.request.attributes.get('_route') starts with 'admin.category.' ? 'active' : '' }}"
                  href="{{ path('admin.category.index') }}">
                  Catégories
                </a>
              </li>

              <!-- Partager une recette -->
              {% if app.user %}
              <li class="nav-item">
                <a class="nav-link"
                  href="{{ path('recipe_new')}}">
                  Contribution
                </a>
              </li>
              {% endif %}

              <!-- Administration -->
              {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MANAGER') %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path('admin.recipe.index') }}">Administration</a>
              </li>
              {% endif %}
            </ul>


            <!-- Utilisateur connecté / non connecté -->
            <div class="d-flex align-items-center nav-user-text">
              {% if app.user %}
                <div class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown"
                    aria-expanded="false">
                    {{ app.user.username|default('Profil')|title }}
                  </a>
                  <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                    <li><a class="dropdown-item" href="#">Mon profil</a></li>
                    <li><a class="dropdown-item" href="#">Mes recettes</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
                  </ul>
                </div>
              {% else %}
                <a href="{{ path('app_login') }}" class="btn btn-outline-primary me-2">Connexion</a>
                <a href="{{ path('app_register') }}" class="btn btn-primary">Inscription</a>
              {% endif %}
            </div>
          </div>
        </div>
      </nav>
      

      <!-- Main content -->
      <main>
      {% include 'partials/flash.html.twig' %}
        {% block body %}{% endblock %}
      </main>
      <footer class="mt-5 pt-5 bg-footer text-dark">
      <div class="container">

        <!-- Newsletter -->
        {{ render(controller('App\\Controller\\NewsletterController::renderForm')) }}
      </div>

        <!-- Grille de liens -->
        <div class="row justify-content-center text-center row-cols-1 row-cols-md-4 g-4 pb-5">
          <div class="col">
            <h5 class="text-success">Découvrir</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">Recettes</a></li>
              <li><a href="#" class="footer-link">Catégories</a></li>
              <li><a href="#" class="footer-link">Nouveautés</a></li>
              <li><a href="#" class="footer-link">Saison</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">À propos</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">Notre histoire</a></li>
              <li><a href="#" class="footer-link">L’équipe</a></li>
              <li><a href="#" class="footer-link">Contact</a></li>
              <li><a href="#" class="footer-link">Presse</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">Aide</h5>
            <ul class="list-unstyled">
              <li><a href="#" class="footer-link">FAQ</a></li>
              <li><a href="#" class="footer-link">Support</a></li>
              <li><a href="#" class="footer-link">Mentions légales</a></li>
              <li><a href="#" class="footer-link">Politique de confidentialité</a></li>
            </ul>
          </div>

          <div class="col">
            <h5 class="text-success">Suivez-nous</h5>
            <div class="d-flex justify-content-center gap-3 mt-2">
              <a href="#" class="social-icon"><i class="fab fa-facebook-f"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-instagram"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-tiktok"></i></a>
              <a href="#" class="social-icon"><i class="fab fa-x-twitter"></i></a>
            </div>
          </div>
        </div>
      </div>

      <!-- Bas de page -->
      <div class="text-center py-3 bg-light border-top">
        <small class="text-muted">© {{ "now"|date("Y") }} MonSite Recettes — Tous droits réservés.</small>
      </div>
    </footer>
  </body>
</html>
